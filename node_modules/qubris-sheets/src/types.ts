export interface IQbrisSheetsStyles {
  footerButton?: string;
  footerBar?: string;
  tableWrapperStyle?: string;
  tableStyle?: string;
  cellStyle?: string;
  tableHeaderStyle?: string;
}

export interface IQbrisSheetsOptions {
  handleError?: (error: Error) => void,
  containerSelector?: string,
  renderFooter?: boolean
  styles?: IQbrisSheetsStyles,
  maxRows?: number,
  maxColumns?: number
  renderOnlySheetRange?: boolean;
  onLoading?: (isLoading: boolean) => void;
}