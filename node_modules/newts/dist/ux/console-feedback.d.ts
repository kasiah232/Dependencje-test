import { AsyncFunc, Feedback } from "../types";
export declare class ConsoleFeedback implements Feedback {
    private spinChar;
    run<T>(label: string, action: AsyncFunc<T>): Promise<T>;
    private _started;
    private startTimer;
    private reportTime;
    time<T>(fn: AsyncFunc<T>): Promise<T>;
    private wait;
    private spin;
    private stopWait;
    private nextSpinChar;
    rewrite(pre: string, post: string, padSize?: number, cols?: number): void;
    log(text: string): void;
    warn(text: string): void;
    error(text: string): void;
}
